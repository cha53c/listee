<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
    <style>
        form.remove-list {
            display: inline-block;
        }
        form.remove-list.hide {
            display: none;
        }

    </style>
</head>
<body>
<h1><%= heading %></h1>
<button onclick="window.location.href = '/lists/<%= userId %>/create';">Create List</button>
<button onclick="toggleEdit()">edit</button>

<h2>You have <%= listCount %> lists </h2>
<!-- TODO can you be changed to reuse for list items as well? -->
<% for (const name of listnames) { %>
        <a href="/lists/<%= userId %>/show/<%= name %>"><%= name %></a>
        <%- include('list/removeList', {userId: userId, name: name }); %>
   <p></p>
<% } %>
</body>
<script>
    function toggleEdit() {
      const elements = document.getElementsByClassName('remove-list');
      for (const el of elements){
          el.classList.toggle('hide');
      }
    }
</script>
</html>